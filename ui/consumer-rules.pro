# Iris Shell - consumer ProGuard rules for :ui.
# These rules are applied automatically to any module that depends on :ui.
#
# Compose ships its own consumer rules. Nothing module-specific to add yet.
